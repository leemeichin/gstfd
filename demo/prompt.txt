Use gstfd from SKILL.md. We need a support operations command centre before the next shift starts. Turn tickets.csv into a working browser dashboard: upload a CSV, show the open queue with urgent tickets first and oldest first within a priority, filter by owner, search subjects, and export exactly the visible queue as CSV. The export has messy spacing and casing; quoted commas and quotes must work. Closed/resolved tickets should not enter the open queue. Show useful counts and let me load the provided sample in one click. Make the smallest useful MVP and check it works. Keep everything local, use no external services, and do not deploy or push. The folder starts with only the skill and sample data.
